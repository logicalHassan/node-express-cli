const express = require('express');
const validate = require('../middlewares/validate');
const auth = require('../middlewares/auth');
const {{camelCase name}}Validation = require('../validations/{{fileName}}.validation');
const {{camelCase name}}Controller = require('../controllers/{{fileName}}.controller');

const router = express.Router();

router.route('/').get(auth(), validate({{camelCase name}}Validation.get{{pascalCase name}}), {{camelCase name}}Controller.get{{pascalCase name}});

module.exports = router;