const express = require('express');
const validate = require('../middlewares/validate');
const auth = require('../middlewares/auth');
const {{camelCase name}}Validation = require('../validations/auth.validation');
const {{camelCase name}}Controller = require('../controllers/auth.controller');

const router = express.Router();

router.route('/')
  .get(validate({{camelCase name}}Validation.get{{pascalCase name}}), {{camelCase name}}Controller.get{{pascalCase name}});

module.exports = router;