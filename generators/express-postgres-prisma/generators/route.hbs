import {{camelCase name}}Controller from '@/controllers/{{kebabCase name}}.controller';
import auth from '@/middlewares/auth';
import validate from '@/middlewares/validate';
import {{camelCase name}}Validation from '@/validations/{{kebabCase name}}.validation';
import express from 'express';

const router = express.Router();

router.route('/')
  .get(validate({{camelCase name}}Validation.get{{pascalCase name}}), {{camelCase name}}Controller.get{{pascalCase name}});

export default router;